spring:
  datasource:
    url: jdbc:postgresql://localhost:6005/flowers
    username: postgres
    password: password


  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always